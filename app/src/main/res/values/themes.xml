<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.XMLPractice" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
         <item name="colorPrimary">@color/white</item>
    </style>

    <style name="Theme.XMLPractice" parent="Base.Theme.XMLPractice" />
    <style name="RectangleChip" parent="Widget.MaterialComponents.Chip.Choice">
        <item name="chipCornerRadius">4dp</item>
        <item name="chipMinHeight">36dp</item>
        <item name="android:textColor">#000000</item>
        <item name="chipStrokeWidth">0dp</item>
        <item name="chipBackgroundColor">@drawable/chip_selector</item>
    </style>
</resources>